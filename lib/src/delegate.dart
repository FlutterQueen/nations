// part of 'controller.dart';

// class _NationsDelegate extends LocalizationsDelegate {
//   ///  * use only the lang code to generate i18n file path like en.json or ar.json
//   // final bool useOnlyLangCode;

//   @override
//   bool isSupported(Locale locale) => Nations.supportedLocales.contains(locale);

//   @override
//   Future load(Locale locale) async {
//     log('😏 delegate load called with locale :$locale');
//     await Nations.load(locale);
//   }

//   @override
//   bool shouldReload(LocalizationsDelegate<TranslationData> old) => false;
// }
