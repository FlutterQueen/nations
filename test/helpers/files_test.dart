void main() {
  // TODO :: cant load from the root bundle in test mode
  // test('it loads package assets content if locale is supported', () async {
  //   final content = await loadPackageTranslation(const Locale('en'));
  //   expect(content.length, isNot(0));
  //   expect(content['package_name'], equals('nations'));
  // });
}
